name: Diagnose Tests PR 129

on:
  push:
    branches:
      - codex/trusted-podman-config-observation

permissions:
  contents: write

jobs:
  tests:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: codex/trusted-podman-config-observation
      - name: Publish concise test diagnostics
        run: |
          set +e
          cargo test --locked rootless_podman_config_observation -- --nocapture > /tmp/diagnostic.txt 2>&1
          status=$?
          set -e
          tail -n 160 /tmp/diagnostic.txt > pr-129-test-diagnostic.txt
          printf '\nexit_status=%s\n' "$status" >> pr-129-test-diagnostic.txt
          rm .github/workflows/diagnose-tests-pr-129.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'Publish PR 129 test diagnostic'
          git push origin HEAD:codex/trusted-podman-config-observation
